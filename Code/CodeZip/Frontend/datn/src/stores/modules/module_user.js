import Vue from "vue";
import categoryAPI from "@/apis/views/userAPI";
import userAPI from "@/apis/views/userAPI";

const state = {
};

const getters = {};

const mutations = {
};

const actions = {
};

export default {
  state,
  getters,
  mutations,
  actions,
};
